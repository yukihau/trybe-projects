import React from 'react';

function RegisterRedirectButton() {
  return (
    <button
      type="button"
      id="register-btn"
      data-testid="common_login__button-register"
    >
      Ainda não tenho conta
    </button>
  );
}

RegisterRedirectButton.propTypes = {};

export default RegisterRedirectButton;
